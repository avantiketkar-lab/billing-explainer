Screenshots referenced by ../WALKTHROUGH.md and ../../README.md.

  hero.png       Support engineer: narrated answer, every figure cited, with
                 the tool-call trace beside it
  denial.png     A request for an account outside the session's scope, refused
                 before any read — empty tool-call trace
  validator.png  The narration withheld because citations did not resolve;
                 the computed ranking shown instead

All three are the running application against the seeded fixture, cropped to
the app itself — no IDE chrome, no terminal.
